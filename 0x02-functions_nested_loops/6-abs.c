#include "main.h"

/**
*_abs- computes the absolute value of an integer;
*@h: the int to be checked
*Return: The absolute value of integer
*/

int _abs(int h)
{
if (h < 0)
{
return (-h);
}
return (h);
}
