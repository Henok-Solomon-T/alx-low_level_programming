#include <stdio.h>

/**
 * main- prints the first 50 Fibonacci numbers,
 * starting with 1 and 2, followed by a new line
 * Return: 0 If exist
 */

int main(void)
{
long int a, b, c, next;
b = 1;
c = 2;

for (a = 1; a <+ 50; ++a)
{
if (b !=
}
}
