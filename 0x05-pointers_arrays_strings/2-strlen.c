#include "main.h"

/**
 * _strlen- a function that returns the length of a string
 * @s: string pointer to evaluate to passed to the func
 */

int _strlen(char *s)

{

int a; // a is len

a = 0;
while (s{a} != '\0') 
{
a++;
}

return (a);

}
