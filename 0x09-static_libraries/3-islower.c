#include "main.h"

/**
*_islower- checks for lowercase character,
*@c: the character that will check
*Return: 1 if exisit or lowercase, else 0;
*/

int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
